{
  "name": "avinash-portfolio",
  "version": "0.1.0",
  "private": true,
  "description": "Portfolio site: a single authored HTML source built into docs/ for GitHub Pages, plus the retrieval backend that answers questions about it.",
  "scripts": {
    "build": "node scripts/build-site.mjs",
    "qa": "node scripts/qa-responsive.cjs",
    "rag:index": "node rag/scripts/build-rag-index.mjs",
    "rag:bundle": "node rag/scripts/bundle-worker.mjs"
  },
  "devDependencies": {
    "playwright": "^1.63.0"
  },
  "dependencies": {
    "@gsap/react": "^2.1.2",
    "gsap": "^3.15.0"
  }
}
